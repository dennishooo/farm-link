node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# Official rulebook PDFs are copyright Lookout Games — kept locally as a
# reference for implementing the rules, never redistributed.
docs/rulebook/

# Playwright MCP transient artifacts (screenshots, traces)
.playwright-artifacts/

# Playwright's own output; the committed baselines live in visual/*-snapshots.
/test-results/
/playwright-report/
